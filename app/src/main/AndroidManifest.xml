<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.bemyapp">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".not_found"></activity>
        <activity android:name=".registration_form" />
        <activity android:name=".beFor" />
        <activity android:name=".register" />
        <activity android:name=".update" />
        <activity android:name=".delete" />
        <activity android:name=".update_or_delete" />
        <activity android:name=".conect" />
        <activity android:name=".fromWereToWere" />
        <activity android:name=".chooseContactWay" />
        <activity android:name=".search" />
        <activity android:name=".beMyChoose" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>